spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:h2:mem:krishi_test_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: sa
    password: 
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

app:
  jwt:
    secret: 9876543210abcdef0123456789abcdef0123456789abcdef0123456789abcdef
    expiration-ms: 3600000
  admin:
    mobile: "9999999999"
    email: "admin@smartkrishi.gov.in"
    password: "TestAdmin@SecurePass123"
  ai:
    provider: mock
  weather:
    provider: mock


